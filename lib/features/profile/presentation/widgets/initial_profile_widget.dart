import 'package:flutter/material.dart';

class InitialProfileWidget extends StatelessWidget {
  const InitialProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: SizedBox());
  }
}
